//! Playlist settings: per-playlist user preferences (sort, artwork, search
//! query, visibility, favorites, copy tracking, and manual ordering).

mod crud;
mod position;
mod query_all;
mod sort_artwork;
mod visibility;
