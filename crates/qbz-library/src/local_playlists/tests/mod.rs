//! Test suite for `local_playlists`, split by concern:
//! `common` (shared setup), `playlist_tests` (header CRUD),
//! `add_remove_tests` and `reorder_tests` (membership CRUD).

mod add_remove_tests;
mod common;
mod playlist_tests;
mod reorder_tests;
