// Tab-strip and searchType-radio building blocks for SearchResultsView.

import { Theme } from "../foundation/semantic-colors.slint";

// One tab in the results tab strip.
export component SearchTab inherits Rectangle {
    in property <string> label;
    in property <bool> active;
    callback clicked();

    VerticalLayout {
        spacing: 5px;
        Text {
            text: root.label;
            color: root.active
                ? Theme.text-primary
                : (ta.has-hover ? Theme.text-secondary : Theme.text-muted);
            font-size: 14px;
            font-weight: root.active ? 700 : 500;
        }
        Rectangle {
            height: 2px;
            border-radius: 1px;
            background: root.active ? Theme.accent : transparent;
        }
    }

    ta := TouchArea {
        mouse-cursor: pointer;
        clicked => {
            root.clicked();
        }
    }
}

// A styled searchType filter radio — a ring with an accent dot when
// selected, and a label. Not a native radio.
export component FilterRadio inherits Rectangle {
    in property <string> label;
    in property <bool> selected;
    callback clicked();

    height: 22px;
    width: radio-row.preferred-width;

    radio-row := HorizontalLayout {
        spacing: 6px;
        VerticalLayout {
            alignment: center;
            Rectangle {
                width: 14px;
                height: 14px;
                border-radius: 7px;
                border-width: 1.5px;
                border-color: root.selected ? Theme.accent : Theme.text-muted;
                Rectangle {
                    width: 8px;
                    height: 8px;
                    border-radius: 4px;
                    background: root.selected ? Theme.accent : transparent;
                    x: Math.round((parent.width - self.width) / 2 / 1px) * 1px;
                    y: Math.round((parent.height - self.height) / 2 / 1px) * 1px;
                }
            }
        }
        Text {
            text: root.label;
            color: (root.selected || ta.has-hover)
                ? Theme.text-primary
                : Theme.text-secondary;
            font-size: 12px;
            vertical-alignment: center;
        }
    }

    ta := TouchArea {
        mouse-cursor: pointer;
        clicked => {
            root.clicked();
        }
    }
}
