//! `AlsaDirectStream::new_dop` and `AlsaDirectStream::new_native_dsd` — the
//! two DSD-plan-Phase-2/3 additive constructors, grouped together since they
//! share the DSD context.

mod dop;
mod native_dsd;
